// URL base do backend. Em produção, defina VITE_API_URL no ambiente de build
// (ex.: no Vercel/Netlify) apontando para o Render, ex.: https://validade-api.onrender.com
export const API_BASE = import.meta.env.VITE_API_URL || 'http://localhost:8082';
